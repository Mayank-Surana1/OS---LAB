#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int main(void) {
pid_t pid = fork();
if (pid < 0) {
perror("Fork failed:");
exit(1);
}
if (pid == 0) {
printf("I am the child.\n");
} else {
printf("I am the parent, my child's PID is %d.\n", pid);
}
return 0;
}